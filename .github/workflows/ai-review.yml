# .github/workflows/ai-code-review.yml
name: AI Code Review

on:
  pull_request:
    branches: [ main ]
    types: [opened]   # запускаємо лише при відкритті PR
  workflow_dispatch:   # опційно: ручний запуск з Actions

permissions:
  contents: read
  pull-requests: write

jobs:
  ai_review:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      MODEL: gpt-5-nano                   # <-- заміни на ваш ID моделі
      OPENAI_API_URL: https://api.openai.com/v1/responses
      DIFF_MAX_CHARS: "150000"            # просте обмеження довжини DIFF
    steps:
      # (1) ЧЕКАУТ
      - name: (1) Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # (2) ВИЗНАЧИТИ ДІФИ base…HEAD
      - name: (2) Compute DIFF (base…HEAD, unified=0)
        id: diff
        shell: bash
        run: |
          set -euo pipefail
          BASE_SHA="${{ github.event.pull_request.base.sha }}"
          HEAD_SHA="${{ github.sha }}"

          git diff --unified=0 "$BASE_SHA...$HEAD_SHA" > diff.patch || true

          # Триммінг DIFF для контролю токенів/вартості
          DIFF_CONTENT="$(cat diff.patch || true)"
          echo "raw_len=${#DIFF_CONTENT}" >> $GITHUB_OUTPUT
          printf "%s" "${DIFF_CONTENT:0:${DIFF_MAX_CHARS}}" > diff.trimmed

          if [[ ! -s diff.trimmed ]]; then
            echo "⚠️ Empty diff detected (nothing to review)." > comment.md
            echo "raw_len=0" >> $GITHUB_OUTPUT
          fi
- name: (3) Read AI instruction files
        shell: bash
        run: |
          set -euo pipefail
          PROMPT_PATH=".ai/ai-reviewer-instruction.md"
          RULES_PATH=".ai/review-rules.md"
          [[ -f "$PROMPT_PATH" ]] || { echo "❌ Missing $PROMPT_PATH"; exit 1; }
          [[ -f "$RULES_PATH"  ]] || { echo "❌ Missing $RULES_PATH"; exit 1; }

      - name: (4) Build request.json (safe via jq --rawfile)
        id: build
        shell: bash
        run: |
          set -euo pipefail
          [[ -f diff.trimmed ]] || : > diff.trimmed
          jq -n \
            --arg model "$MODEL" \
            --rawfile sys   ".ai/ai-reviewer-instruction.md" \
            --rawfile rules ".ai/review-rules.md" \
            --rawfile diff  "diff.trimmed" \
            '{
              model: $model,
              response_format: { type: "json_object" },
              input: [
                { "role": "system", "content": $sys },
                { "role": "user",   "content": ("Rules:\n" + $rules + "\n\nDIFF:\n" + $diff) }
              ]
            }' > request.json

      - name: (5) Call OpenAI
        id: openai
        if: steps.diff.outputs.raw_len != '0'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          RESP=$(curl -sS -X POST "$OPENAI_API_URL" \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -H "Content-Type: application/json" \
            --data @request.json)

          # Підтримуємо обидва ендпойнти:
          #  - Responses API: .output_text
          #  - Chat Completions: .choices[0].message.content
          RESULT=$(echo "$RESP" | jq -r '
            .output_text
            // (.choices[0].message.content // "")
            // ""
          ')

          echo "$RESULT" | jq . > ai_result.json 2>/dev/null || echo '{}' > ai_result.json

          {
            echo "### 🤖 AI Code Review"
            echo "_Model: \`${MODEL}\` • raw diff: ${{ steps.diff.outputs.raw_len }} chars_"
            echo
            echo "\`\`\`json"
            cat ai_result.json
            echo "\`\`\`"
          } > comment.md

      - name: (6) Create/Update sticky comment
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: ai-code-review
          path: comment.md
